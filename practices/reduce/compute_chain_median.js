'use strict';

function compute_chain_median(collection) {
  //在这里写入代码
  // var array = [];
  // for(var i = 0;i < collection.length;i++) {
  //    if(collection[i]+1)
  // }
  // return array;
}

module.exports = compute_chain_median;


//
// if(parseInt(collection[i]) === parseInt(collection[i])) {
//
//     array.push(parseInt(collection[i]));
// }
